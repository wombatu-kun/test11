spring:
  application:
    name: eurekaserver
  cloud:
    config:
      uri: http://localhost:8081

logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    wombatukun.tests: DEBUG